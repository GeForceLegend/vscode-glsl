(
	EmitMeshTasksEXT|
	SetMeshOutputsEXT|
	(assume|expect)EXT|
	atomic(Load|Store)|
	bfloat16BitsTo(I|Ui)ntEXT|
	bitcastQCOM|
	clockRealtime(2x32)?EXT|
	colorAttachmentReadEXT|
	computeDir|
	controlBarrier|
	coopmatToVectorQCOM|
	coopMat(Load|MulAdd|Store)(NV)?|
	coopMat((Load|Store)Tensor|PerElement|Reduce|Transpose)NV|
	coopVec(Load|MatMul(Add)?|(OuterProduct|ReduceSum)Accumulate|Store)NV|
	createTensor(Layout|View)NV|
	dot(Packed4x8)?(AccSat)?EXT|
	doubleBitsTo(Ui|I)nt64|
	executeCallableNV|
	extractSubArrayQCOM|
	fetchMicroTriangleVertex(Barycentric|Position)NV|
	(float16|half)BitsTo(Ui|I)nt16|
	floate(5m2|4m3)BitsTo(Ui|I)ntEXT|
	fragment(Mask)?FetchAMD|
	hitObject(
		Get(
			ClusterId|
			LSS(Positions|Radii)|
			Sphere(Position|Radius)|
		)|
		RecordHit(WithIndex)?(Motion)?|
	)NV|
	hitObject(
		Get(
			IntersectionTriangleVertexPositions|
			RayFlags|
		)|
		RecordFromQuery|
		(Trace)?(Motion)?ReorderExecute
	)EXT|
	hitObject(
		ExecuteShader|
		Get(
			Attributes|
			CurrentTime|
			(Geometry|Primitive)Index|
			HitKind|
			Instance(CustomIndex|Id)|
			Object(Ray(Direction|Origin)|ToWorld)|
			RayT(Max|Min)|
			Shader(BindingTableRecordIndex|RecordBufferHandle)|
			World(Ray(Direction|Origin)|ToObject)
		)|
		Is(Empty|Hit|Miss)|
		Record(Empty|Miss(Motion)?)|
		TraceRay(Motion)?
	)(EXT|NV)|
	ignoreIntersectionNV|
	imageAtomic(Load|Store)|
	pack(16|32|64|(Float|Uint)2x16|(Ui|I)nt2x32|Uint4x16)|
	rayQueryGetIntersection(
		ClusterId|
		LSS(HitValue|Positions|Radii)|
		Sphere(Position|Radius)
	)NV|
	rayQuery(
		(Confirm|Generate)Intersection|
		GetIntersection(
			Barycentrics|
			CandidateAABBOpaque|
			FrontFace|
			(Geometry|Primitive)Index|
			Instance(CustomIndex|Id|ShaderBindingTableRecordOffset)|
			Object(Ray(Direction|Origin)|ToWorld)|
			T|
			TriangleVertexPositions|
			Type|
			WorldToObject
		)|
		Get(Ray(Flags|TMin)|WorldRay(Direction|Origin))|
		Initialize|
		Proceed|
		Terminate
	)EXT|
	reorderThread(EXT|NV)|
	reportIntersection(EXT|NV)|
	saturatedConvertEXT|
	setTensor(Layout(BlockSize|ClampValue|Dimension|Stride)|View(Clip|Dimensions|Stride))NV|
	sliceTensorLayoutNV|
	subgroup(
		All(Equal)?|
		Any|
		Ballot((Exclusive|Inclusive)?BitCount|BitExtract|Find[LM]SB)?|
		Barrier|
		Broadcast(First)?|
		(Clustered|Exclusive|Inclusive)?(Add|And|Max|Min|Mul|Or|Xor)|
		(Clustered)?Rotate|
		Elect|
		InverseBallot|
		MemoryBarrier(Buffer|Image|Shared)?|
		PartitionNV|
		Partitioned(Exclusive|Inclusive)?(Add|And|Max|Min|Mul|Or|Xor)NV|
		Quad(All|Any|Broadcast|Swap(Diagonal|Horizontal|Vertical))|
		Shuffle(Down|Up|Xor)?
	)|
	terminateRayNV|
	texture(BlockMatch(Windows|Gather)?S[AS]D|BoxFilter|Weighted)QCOM|
	textureFootprint(Clamp|Lod|Grad(Clamp)?)?NV|
	trace(NV|Ray(EXT|MotionNV))|
	u?int16BitsTo(Float16|Half)|
	u?int64BitsToDouble|
	u?intBitsTo(BFloat16|Floate(5m2|4m3))EXT|
	unpack(8|16|32|(Float|Uint)2x16|(Ui|I)nt2x32|Uint4x16)|
	vectorToCoopmatQCOM|
	writePackedPrimitiveIndices4x8NV
)